const initialState = {
    session: 25,
    break: 5,
    timeleft: "25:00", 
    running: false
};

const appReducer = (state = initialState, action) => { 
    return state;
};

export default appReducer;